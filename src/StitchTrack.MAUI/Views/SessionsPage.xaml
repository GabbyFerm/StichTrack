<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewModels="clr-namespace:StitchTrack.Application.ViewModels;assembly=StitchTrack.Application"
             x:Class="StitchTrack.MAUI.Views.SessionsPage"
             x:DataType="viewModels:SessionsViewModel"
             BackgroundColor="{AppThemeBinding Light={StaticResource LightPageBackground}, Dark={StaticResource DarkPageBackground}}"
             Shell.NavBarIsVisible="False">

  <Grid RowDefinitions="Auto,Auto,*">

    <!-- TOP BAR: Logo-->
    <Grid Grid.Row="0"
          BackgroundColor="{AppThemeBinding Light={StaticResource LightAppBackground}, Dark={StaticResource DarkAppBackground}}"
          Padding="30,30,30,30">

      <Image Source="logo.svg"
             HeightRequest="35"
             HorizontalOptions="Start"
             Aspect="AspectFit" />

    </Grid>

    <!-- HEADER: Icon + Title -->
    <Grid Grid.Row="1"
          BackgroundColor="{AppThemeBinding Light={StaticResource LightAppBackground}, Dark={StaticResource DarkAppBackground}}"
          Padding="30,10,20,20">

      <HorizontalStackLayout Spacing="10" HorizontalOptions="Start">
        <Image Source="session.svg"
               WidthRequest="22"
               HeightRequest="22"
               VerticalOptions="Center">
          <Image.Behaviors>
            <toolkit:IconTintColorBehavior TintColor="White" />
          </Image.Behaviors>
        </Image>
        <Label Text="YOUR STATS"
               Style="{StaticResource HeaderTitle}"
               VerticalOptions="Center" />
      </HorizontalStackLayout>

    </Grid>

    <!-- SCROLLABLE CONTENT -->
    <ScrollView Grid.Row="2">

      <VerticalStackLayout Spacing="0" Margin="0,16,0,0">

        <!-- Tab labels -->
        <Grid Padding="16,0,16,10" ColumnDefinitions="*,*,*,*">

          <Label Grid.Column="0" Text="Today" Style="{StaticResource FontMedium}" FontSize="14"
           HorizontalOptions="Center"
           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}">
            <Label.Triggers>
              <DataTrigger TargetType="Label" Binding="{Binding IsTodaySelected}" Value="True">
                <Setter Property="TextColor" Value="{StaticResource BrandGold}" />
                <Setter Property="FontFamily" Value="MontserratSemiBold" />
              </DataTrigger>
            </Label.Triggers>
            <Label.GestureRecognizers>
              <TapGestureRecognizer Command="{Binding ShowTodayCommand}" />
            </Label.GestureRecognizers>
          </Label>

          <Label Grid.Column="1" Text="Week" Style="{StaticResource FontMedium}" FontSize="14"
           HorizontalOptions="Center"
           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}">
            <Label.Triggers>
              <DataTrigger TargetType="Label" Binding="{Binding IsThisWeekSelected}" Value="True">
                <Setter Property="TextColor" Value="{StaticResource BrandGold}" />
                <Setter Property="FontFamily" Value="MontserratSemiBold" />
              </DataTrigger>
            </Label.Triggers>
            <Label.GestureRecognizers>
              <TapGestureRecognizer Command="{Binding ShowThisWeekCommand}" />
            </Label.GestureRecognizers>
          </Label>

          <Label Grid.Column="2" Text="Month" Style="{StaticResource FontMedium}" FontSize="14"
           HorizontalOptions="Center"
           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}">
            <Label.Triggers>
              <DataTrigger TargetType="Label" Binding="{Binding IsThisMonthSelected}" Value="True">
                <Setter Property="TextColor" Value="{StaticResource BrandGold}" />
                <Setter Property="FontFamily" Value="MontserratSemiBold" />
              </DataTrigger>
            </Label.Triggers>
            <Label.GestureRecognizers>
              <TapGestureRecognizer Command="{Binding ShowThisMonthCommand}" />
            </Label.GestureRecognizers>
          </Label>

          <Label Grid.Column="3" Text="All" Style="{StaticResource FontMedium}" FontSize="14"
           HorizontalOptions="Center"
           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}">
            <Label.Triggers>
              <DataTrigger TargetType="Label" Binding="{Binding IsAllSelected}" Value="True">
                <Setter Property="TextColor" Value="{StaticResource BrandGold}" />
                <Setter Property="FontFamily" Value="MontserratSemiBold" />
              </DataTrigger>
            </Label.Triggers>
            <Label.GestureRecognizers>
              <TapGestureRecognizer Command="{Binding ShowAllCommand}" />
            </Label.GestureRecognizers>
          </Label>

        </Grid>

        <!-- Underlines — gold for active, dark for inactive -->
        <Grid Padding="16,0" ColumnDefinitions="*,*,*,*" ColumnSpacing="0">

          <BoxView Grid.Column="0" HeightRequest="2" HorizontalOptions="Fill"
             Color="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}">
            <BoxView.Triggers>
              <DataTrigger TargetType="BoxView" Binding="{Binding IsTodaySelected}" Value="True">
                <Setter Property="Color" Value="{StaticResource BrandGold}" />
              </DataTrigger>
            </BoxView.Triggers>
          </BoxView>

          <BoxView Grid.Column="1" HeightRequest="2" HorizontalOptions="Fill"
             Color="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}">
            <BoxView.Triggers>
              <DataTrigger TargetType="BoxView" Binding="{Binding IsThisWeekSelected}" Value="True">
                <Setter Property="Color" Value="{StaticResource BrandGold}" />
              </DataTrigger>
            </BoxView.Triggers>
          </BoxView>

          <BoxView Grid.Column="2" HeightRequest="2" HorizontalOptions="Fill"
             Color="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}">
            <BoxView.Triggers>
              <DataTrigger TargetType="BoxView" Binding="{Binding IsThisMonthSelected}" Value="True">
                <Setter Property="Color" Value="{StaticResource BrandGold}" />
              </DataTrigger>
            </BoxView.Triggers>
          </BoxView>

          <BoxView Grid.Column="3" HeightRequest="2" HorizontalOptions="Fill"
             Color="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}">
            <BoxView.Triggers>
              <DataTrigger TargetType="BoxView" Binding="{Binding IsAllSelected}" Value="True">
                <Setter Property="Color" Value="{StaticResource BrandGold}" />
              </DataTrigger>
            </BoxView.Triggers>
          </BoxView>

        </Grid>

        <!-- STATS CARDS: 2x2 grid -->
        <Grid ColumnDefinitions="*,*"
              RowDefinitions="Auto,Auto"
              ColumnSpacing="12"
              RowSpacing="12"
              Margin="16,16,16,16">

          <!-- Total Time -->
          <Border Grid.Row="0" Grid.Column="0"
                  Padding="16,20"
                  StrokeThickness="0"
                  BackgroundColor="{StaticResource ButtonSecondary}">
            <Border.StrokeShape>
              <RoundRectangle CornerRadius="12" />
            </Border.StrokeShape>
            <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
              <Label Text="{Binding TotalTime}"
                     Style="{StaticResource FontBold}"
                     FontSize="28"
                     TextColor="{StaticResource BrandGold}"
                     HorizontalOptions="Center" />
              <Label Text="Total Time"
                     Style="{StaticResource FontMedium}"
                     FontSize="13"
                     TextColor="White"
                     Opacity="0.85"
                     HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>

          <!-- Sessions count -->
          <Border Grid.Row="0" Grid.Column="1"
                  Padding="16,20"
                  StrokeThickness="0"
                  BackgroundColor="{StaticResource ButtonSecondary}">
            <Border.StrokeShape>
              <RoundRectangle CornerRadius="12" />
            </Border.StrokeShape>
            <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
              <Label Text="{Binding SessionCount}"
                     Style="{StaticResource FontBold}"
                     FontSize="28"
                     TextColor="{StaticResource BrandGold}"
                     HorizontalOptions="Center" />
              <Label Text="Sessions"
                     Style="{StaticResource FontMedium}"
                     FontSize="13"
                     TextColor="White"
                     Opacity="0.85"
                     HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>

          <!-- Avg Session -->
          <Border Grid.Row="1" Grid.Column="0"
                  Padding="16,20"
                  StrokeThickness="0"
                  BackgroundColor="{StaticResource ButtonSecondary}">
            <Border.StrokeShape>
              <RoundRectangle CornerRadius="12" />
            </Border.StrokeShape>
            <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
              <Label Text="{Binding AvgSessionDuration}"
                     Style="{StaticResource FontBold}"
                     FontSize="28"
                     TextColor="{StaticResource BrandGold}"
                     HorizontalOptions="Center" />
              <Label Text="Avg Session"
                     Style="{StaticResource FontMedium}"
                     FontSize="13"
                     TextColor="White"
                     Opacity="0.85"
                     HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>

          <!-- Total Rows -->
          <Border Grid.Row="1" Grid.Column="1"
                  Padding="16,20"
                  StrokeThickness="0"
                  BackgroundColor="{StaticResource ButtonSecondary}">
            <Border.StrokeShape>
              <RoundRectangle CornerRadius="12" />
            </Border.StrokeShape>
            <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
              <Label Text="{Binding TotalRowsCompleted}"
                     Style="{StaticResource FontBold}"
                     FontSize="28"
                     TextColor="{StaticResource BrandGold}"
                     HorizontalOptions="Center" />
              <Label Text="Total Rows"
                     Style="{StaticResource FontMedium}"
                     FontSize="13"
                     TextColor="White"
                     Opacity="0.85"
                     HorizontalOptions="Center" />
            </VerticalStackLayout>
          </Border>

        </Grid>

        <!-- MOST ACTIVE DAY CARD -->
        <Border Padding="16,14"
                Margin="16,0,16,20"
                StrokeThickness="0"
                BackgroundColor="{AppThemeBinding Light={StaticResource ButtonSecondary}, Dark={StaticResource DarkCardBackground}}"
                IsVisible="{Binding HasMostActiveDay}">
          <Border.StrokeShape>
            <RoundRectangle CornerRadius="12" />
          </Border.StrokeShape>

          <HorizontalStackLayout Spacing="12" VerticalOptions="Center">

            <Image Source="sparkles.svg"
                   WidthRequest="24"
                   HeightRequest="24"
                   VerticalOptions="Center">
              <Image.Behaviors>
                <toolkit:IconTintColorBehavior TintColor="{StaticResource BrandGold}" />
              </Image.Behaviors>
            </Image>

            <VerticalStackLayout Spacing="2" VerticalOptions="Center">
              <Label Text="Most Active Day:"
                     Style="{StaticResource FontBold}"
                     FontSize="14"
                     TextColor="White" />
              <Label Text="{Binding MostActiveDay}"
                     Style="{StaticResource FontRegular}"
                     FontSize="13"
                     TextColor="White"
                     Opacity="0.85" />
            </VerticalStackLayout>

          </HorizontalStackLayout>

        </Border>

        <!-- RECENT SESSIONS -->
        <VerticalStackLayout Margin="16,0,16,4" Spacing="8">
          <Label Text="Recent Sessions"
             Style="{StaticResource FontBold}"
             FontSize="18"
             TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}" />
          <BoxView HeightRequest="1"
               BackgroundColor="{StaticResource BrandGold}"
               HorizontalOptions="Fill"
               Opacity="0.4" />
        </VerticalStackLayout>

      <!-- Empty state -->
        <VerticalStackLayout Padding="20,40"
                             Spacing="12"
                             IsVisible="{Binding IsEmpty}"
                             HorizontalOptions="Center">
          <Label Text="No sessions yet."
                 Style="{StaticResource FontSemiBold}"
                 FontSize="16"
                 TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"
                 HorizontalOptions="Center" />
          <Label Text="Start a timer on any project to track your progress."
                 Style="{StaticResource FontRegular}"
                 FontSize="14"
                 TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"
                 HorizontalOptions="Center"
                 HorizontalTextAlignment="Center" />
        </VerticalStackLayout>

        <!-- Sessions list -->
        <CollectionView ItemsSource="{Binding RecentSessions}"
                        Margin="16,0,16,32"
                        IsVisible="{Binding HasSessions}">
          <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="viewModels:SessionDisplayItem">

              <VerticalStackLayout Spacing="0">

                <!-- Session row -->
                <Grid ColumnDefinitions="Auto,*"
                      ColumnSpacing="12"
                      Padding="0,12,0,10">

                  <!-- Project image or fallback -->
                  <Grid Grid.Column="0"
                        WidthRequest="60"
                        HeightRequest="60"
                        VerticalOptions="Start">
                    <Border StrokeThickness="0"
                            BackgroundColor="{AppThemeBinding Light=#E5E7EB, Dark=#374151}">
                      <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8" />
                      </Border.StrokeShape>

                      <!-- Grid allows two overlapping images inside one Border -->
                      <Grid>
                        <Image Source="project_placeholder.jpg"
                               Aspect="AspectFill"
                               IsVisible="{Binding HasImage, Converter={StaticResource InvertedBoolConverter}}" />
                        <Image Source="{Binding ImagePath}"
                               Aspect="AspectFill"
                               IsVisible="{Binding HasImage}" />
                      </Grid>

                    </Border>
                  </Grid>

                  <!-- Session details -->
                  <VerticalStackLayout Grid.Column="1" Spacing="2">
                    <Label Text="{Binding ProjectName}"
                           Style="{StaticResource FontSemiBold}"
                           FontSize="14"
                           TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}" />
                    <Label Text="{Binding DateText}"
                           Style="{StaticResource FontRegular}"
                           FontSize="12"
                           TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}" />
                    <Label Text="{Binding RowsText}"
                           Style="{StaticResource FontRegular}"
                           FontSize="12"
                           TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"
                           IsVisible="{Binding HasRows}" />
                  </VerticalStackLayout>

                </Grid>

                <!-- Divider between sessions -->
                <BoxView HeightRequest="1"
                         BackgroundColor="{StaticResource BrandGold}"
                         HorizontalOptions="Fill"
                         Opacity="0.4" />

              </VerticalStackLayout>

            </DataTemplate>
          </CollectionView.ItemTemplate>
        </CollectionView>

        <Label Text="Showing 10 most recent sessions"
               Style="{StaticResource FontRegular}"
               FontSize="12"
               HorizontalOptions="Center"
               Opacity="0.6"
               IsVisible="{Binding HasMoreSessions}"
               TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"
               Margin="0,0,0,16" />

      </VerticalStackLayout>

    </ScrollView>

  </Grid>

</ContentPage>
